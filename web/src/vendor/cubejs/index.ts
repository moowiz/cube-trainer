// cubejs, vendored (see ./README.md): the cube model plus the two-phase solver,
// as one ESM module. `import Cube from '.../vendor/cubejs'` is what the npm
// package's default export used to give.
import Cube from './cube.js';
import './solve.js';

export default Cube;
