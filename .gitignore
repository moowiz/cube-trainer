node_modules/
dist/
# fp32 model stays out of git; the small int8 one IS committed so the Pages
# deploy can serve it (DECISION: overrides the original "models gitignored"
# note in CLAUDE.md - the phone test needs the deployed model).
web/public/models/*.onnx
!web/public/models/facekp.onnx
!web/public/models/cubebox.onnx
web/public/ort/
model/data/
model/data_v3/
model/data_v4*/
model/preview_v2/
# hand-labeled real photos: personal photos, kept out of the public repo
# (back them up separately - the labels are hours of hand work)
model/data_real/
model/data_real_val/
stephens_photos/
# stray labeler downloads land in the root before being archived
labels-all*.json
model/.venv/
model/backgrounds/
model/train/runs/
model/export/out/
__pycache__/
*.local
.DS_Store
model/roboflow/
model/preview_v*/
